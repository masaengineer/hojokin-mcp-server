services:
  - type: web
    name: jgrants-mcp-server
    env: python
    rootDir: jgrants-mcp-server
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn jgrants_mcp_server.openapi_server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /docs
    autoDeploy: true
